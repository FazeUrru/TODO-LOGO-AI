# Base de datos SQLite (Prisma). Ruta relativa al esquema en prisma/
DATABASE_URL="file:./db/custom.db"

# Las credenciales del SDK de IA (z-ai-web-dev-sdk) se resuelven en el entorno
# de ejecución; en despliegues propios define las variables que el SDK requiera.
